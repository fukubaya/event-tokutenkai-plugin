# Print Design & Programmable DTP Rules

チラシ、ポスター、帳票、表組み、カタログなど「印刷またはPDF出力」を前提としたデザインタスクにおける行動原則です。

## 1. 原則: 画像生成AIでの文字・表の一発出し禁止
- 画像生成AI（Diffusionモデル等）を使って、文字や数字、表を含む1枚画像を生成して完結させてはならない。文字の崩れや誤字、罫線の歪み、編集不能のリスクを回避するため。
- キービジュアル（背景やイラスト素材）のみを画像生成AI（またはSVG）で用意し、**文字・フォント・表・レイアウトはコード（HTML/CSS または Typst）として記述・分離**すること。

## 2. デザイン・レイアウトの分離とトークン管理
- **デザイントークン（カラーセット、フォント、余白）の分離**:
  - HTML/CSSの場合: `theme.css` 等で `:root` に CSS変数（カスタムプロパティ）を定義し、カラーパレットや余白（`--page-margin`, `--color-primary` など）を一括変更可能にすること。
  - Typstの場合: ファイル冒頭または設定モジュールで `#let primary-color = ...`, `#let page-margin = ...` を定義すること。
- **印刷サイズと裁ち落としの明示**:
  - 必ず `@page` または `#set page(...)` で物理サイズ（A4, B5, mm単位）と余白を明示すること。
  - 商業印刷向けの場合は裁ち落とし（bleed: 3mm）やトンボ（crop marks）を意識した設定を設けること。

## 3. 編集性とベクター品質の担保
- **テキストと表**:
  - ラスタ画像化せず、常にテキスト要素（HTMLタグ `<p>`, `<h1>`, `<table>` または Typst構文）として保持すること。
- **イラスト・図版**:
  - 可能な限りSVGなどのベクター形式を使用し、拡大・印刷に耐えうる解像度非依存の形式にすること。

## 4. 告知ポスター・フライヤー向けデザイン指針
- **主客の明確化（来場者の最大関心事を中心としたレイアウト）**:
  - イベント告知における来場者の主たる関心事（フリーライブや特典会の内容・くじ賞品・チーム分けなど）を紙面の**主役**として扱い、最も広い面積と強い視覚的アクセントを割り当てること。
  - 準備・事務的な情報（物販ルール、入場方法、アクセス、注意事項等）は脇役としてコンパクトに整理・グループ化し、主役の視認性を邪魔しないように配置すること。
- **用紙端まで行き渡る全面背景（フチなし印刷対応）**:
  - 背景画像を使用する場合、`@page { margin: 0; }` を指定し、`body` やルートコンテナに `background-size: cover;` を適用した上で、内側のコンテンツラッパーにパディングを設定すること。中途半端な「周囲の白フチ」が発生しないように設計すること。
- **大胆なジャンプ率（文字のメリハリ）**:
  - すべての文字を均一なサイズにせず、タイトル・見出し（20〜26pt）、特典会やライブの目玉情報（11〜15pt）、本文・説明（8〜9pt）、注意事項・注釈（6.5〜7pt）と、重要度に応じた明確な階層化（ジャンプ率）をつけること。
- **重要情報（表組み等）のスペース確保**:
  - 表組み（特典会のくじ一覧、料金表等）の横に装飾画像を並べて幅を圧迫してはならない。表組みは横幅いっぱいに確保し、装飾画像は見出し横やチーム紹介エリアなどの自然なスペースにスタンプ・バッジとして配置すること。
- **時間経過（時系列順）の厳格な維持と視覚的差別化**:
  - タイムラインは来場者の当日の行動導線に合わせ、**必ず時間経過の順（例: 10:30物販 ➔ 12:25入場 ➔ 13:00ライブ ➔ 13:45特典会）で上から下へ配置し、順序を絶対に前後させないこと**。
  - 主客のメリハリは順序ではなく、「色（鮮やかなイエローやレッド）」や「カードの高さ・パディング・フォントサイズなどの占有スペースの大きさ」によって明確に目立たせること。
- **詳細情報ブロック（特典会等）の余白最適化（デッドスペースの排除）**:
  - 特典会などの大区画カード内で無駄な縦横の余白（隙間）が生じないよう、メニュー、チーム分け、案内マスコットなどを均等グリッド（3カラム等）で整然と並べ、下部に横幅100%のワイドテーブルを隙間なく配置してスペースを余すことなく有効活用すること。
- **タイムテーブルの時刻表示（ピルバッジ）のサイズ・フォント統一**:
  - 各ステップの時刻バッジ（幅、フォントサイズ、パディング等）は紙面全体で厳密に同一（例: 横幅26mm固定、時刻11pt、ラベル7pt）に統一すること。文字数や内容の違いでサイズがバラバラになると視線誘導が乱れ、素人感が出てしまう。
- **タイムテーブル項目間の隙間（デッドスペース）排除と空間演出**:
  - タイムテーブル各項目の間に不自然な縦の隙間が空いてスカスカにならないよう、各項目の情報（料金、出演者、アピール文等）を適切に展開し、目玉項目（ミニライブ等）には関連イラストやステッカーバッジを添えて紙面全体の密度とポップな楽しさを両立させること。
- **参照元URLと印刷用ベクターQRコードの配置原則**:
  - Webの一次情報や最新スケジュールへのアクセスを担保するため、紙面下部等に参照元URLとQRコードを配置すること。
  - 印刷物での読み取り精度を担保するため、QRコードはラスタ画像（PNG等）ではなく**ベクター形式（SVG）**で生成し（例: `npx qrcode "<URL>" -t svg -o qr.svg`）、拡大縮小でも劣化しないようにすること。
  - カメラ読み取りの信頼性のため、物理サイズは最小でも `15mm × 15mm`（推奨 `16〜20mm`）を確保し、十分なクワイエットゾーン（白余白）を設けること。また、カメラが使えない環境やURL直接確認に備えてURLテキストも必ず併記すること。
- **印刷用CSSにおける `box-shadow` / `drop-shadow` / `mix-blend-mode` の禁止（アーティファクト・背景透過漏れ防止）**:
  - Chromium/Puppeteer等のPDFレンダラー（Skia）において、CSSの `box-shadow`、`filter: drop-shadow`、`mix-blend-mode`（例: multiply）は、PDF出力時に半透明な四角いバウンディングボックスや、レイヤー分離・タイリング境界における細長い背景画像の透過漏れ（スリット状のシーム）を引き起こす。
  - 印刷用PDFではこれらを完全排除し、カード背景にはプレーンなソリッドカラー（白 `#ffffff` 等）を使用し、クッキリとした `border`（境界線）やコントラストによって立体感と視認性を担保すること。
  - 白背景のイラスト画像（ステッカー等）は、ブレンドモードに頼らず白背景のカード上に直接配置することで、エッジを美しく同化させつつ透過バグを完全に防止できる。

- **開催場所・会場情報の配置原則（ヘッダー統合）**:
  - イベント告知において最重要情報の一つである「会場・開催場所」は、タイムテーブルの時刻列と混同しないよう、**ヘッダーカード内に統合するか、ヘッダー直下に上方に配置**すること。
  - 施設名、フロア名、注意書き（待機不可・時間厳守）を整理して配置し、タイムライン（物販・入場・ライブ・特典会）との主従関係を明確にすること。

- **ヘッダー内Flexレイアウトのオーバーフロー防止原則**:
  - ヘッダー等で左右分割フレックス配置（タイトル・会場情報 vs 日付・開始時刻バッジ）を行う際、テキスト要素（会場名、住所、注意事項等）に `white-space: nowrap` を多用すると内在幅が紙幅を超え、右側の要素（日付バッジ等）を用紙外へ押し出す原因となる。
  - 左側コンテナには `flex: 1 1 auto; min-width: 0;` を指定し、住所などの冗長な表記を簡潔化（例: 施設名＋フロアのみ）してフォントサイズを最適化し、紙面幅内に確実に収めること。

- **初回作成時における複数イメージ／配色のパターン提示**:
  - クライアントやユーザーへの初回提案時は、単一のデザインに絞らず、世界観や受ける印象が異なる「複数パターン（3パターン程度）」を同時に設計・提示すること。
  - トークン分離アーキテクチャ（HTML構造と `theme-*.css` の分離）を徹底することで、共通のセマンティックHTMLを活かしたまま、低コストで配色・タイポグラフィ・演出を切り替えたバリエーションを提供できる。

- **フォントの脱・画一化と世界観に応じたタイポグラフィ選定**:
  - 「ポップだから丸ゴシック」と画一的に決めつけず、対象グループや作品・イベントのコンセプトやトーン＆マナーに最も合致するフォントスタックを選定すること。
  - **公式リサーチの徹底**: 公式サイトのInspectや楽曲・ビジュアル資料を調査し、公式が採用しているWebフォント（例: `Shippori Mincho`, `Jost`, `Cormorant Garamond` 等）やグループコンセプト（例: ロック、天使、ストリート等）を積極的に取り入れること。
  - **タイポグラフィのバリエーション例**:
    - **エッジの効いたモダン角ゴシック + ジオメトリック欧文**（例: `M PLUS 1p` + `Jost`）: ポップ＆ロック、洗練されたモダンカルチャー
    - **優美なしっぽり明朝 + クラシカルセリフ欧文**（例: `Shippori Mincho` + `Cormorant Garamond`）: 天使・透明感・愛・気品・幻想的な世界観
    - **極太角ゴシック + コンデンスド欧文**（例: `Noto Sans JP 900` + `Oswald`）: ライブハウス、フェス、タワーレコード風の熱気とインパクト
  - **Google Fonts のウェイト最小化原則（Vivliostyle CLIクラッシュ防止）**:
    - 日本語Webフォント（特に `Shippori Mincho` など）を Vivliostyle 等のヘッドレスChromiumベースのレンダラーで読み込む際、ウェイトを多数（500, 700, 800等）指定すると分割.woff2ファイルの過剰フェッチにより内部タイムアウト（`Page.printToPDF: Printing failed`）が発生する。印刷用HTMLでは、必要なウェイトを最小限（例: `wght@700` のみ）に絞ること。

## 5. AI生成画像の安全かつ効果的な部分利用

- 人物や文字をAIに描かせると不自然さや文字崩れが発生するため、AI画像生成は以下の用途に限定して活用すること:
  - **背景テクスチャ・風景**（例: 季節を感じる水彩画風の空や装飾フレーム、文字を邪魔しない明るい背景）
  - **ワンポイントのマスコット・アイコン**（例: ステッカー風のキャラクターやオブジェクト）
- 生成した画像はベクターのHTML/CSSレイアウトの「背景」や「挿絵」として重ね合わせることで、破綻のない高品質なデザインを実現する。

## 6. 推奨CLIツールの利用と高解像度検証
- PDFの生成・コンパイルは、ユーザーの指定環境（`npx`, `uv`, `go`, `gws`）に沿ったCLIコマンドで実行可能な形式を提示すること。
  - **HTML/CSS**: `npx @vivliostyle/cli build <html-file> -o <pdf-file>` または `uv run weasyprint <html-file> <pdf-file>`
  - **Typst**: `npx typst-cli compile <typ-file> <pdf-file>`
- **画像検証時の解像度担保（4Kディスプレイ・300dpi相当）**:
  - 生成されたPDFを目視確認する際、文字潰れを防ぐため高解像度でPNGを出力すること（macOSの場合: `qlmanage -t -s 3508 -o <dir> <pdf-file>` または Swift PDFKitレンダリングにより A4 300dpi相当の 2480×3508px でレンダリング可能）。

## 7. イベント現場即応のための情報構造化原則

- **会場レイアウト図（フロアマップ）の「入場案内枠」統合**:
  - 会場フロア図（ステージ、優先観覧エリア、女性専用エリア、入場導線等）は、開催場所よりも **【観覧エリア入場案内（整理番号順入場）】の枠内に配置** すること。入場順、女性専用エリア、車椅子スペース、入場導線などの図面情報は、実際の入場集合案内の文脈と直接リンクして初めて現場で活きる。
  - D2（`d2` CLI）やベクターSVG（`floor_map.svg`）として自作・再構成し、印刷時にも文字が一切潰れないベクター品質を担保すること。
- **長文行間とレギュレーションのタグ・ピルバッジ化**:
  - 特典会の参加条件やレギュレーション（人数、スマホ限定、1レーン開催、代行撮影可否など）は、単に太字にするだけでなく、**ピルバッジ（カラータグ）化・色分け** を行い、文章の行間（`line-height: 1.4〜1.55`）をゆったり広げること。文章が詰まると現場の混乱を招く。
- **「撮可TIME」と「撮影会」の厳格な分離原則**:
  - **撮可TIME（ライブ中）** と **撮影会（特典会）** は完全に別レギュレーションである。
    - **撮可TIME**: ミニライブ中の撮影許可（一般に一眼レフ・ミラーレス可、静止画のみ、動画・録音・フラッシュ・連写禁止）。
    - **撮影会（特典会）**: 特典会で行われる写真・動画撮影（一般にスマートフォン・タブレット限定、一眼レフ・コンデジ不可）。
  - これらを絶対に混同・混同表記せず、バッジおよび注意事項で明確に分離して表記すること。
- **販売・参加レギュレーション（ループ可否・購入上限）のタイムライン直下配置**:
  - 物販・特典会における「ループ可否（1会計のみ / 買い増し並び直し不可）」「購入上限数」「制限解除のアナウンス方法（公式SNS等）」は来場者の現場行動における最重要情報である。下部の注意事項欄に埋もれさせず、商品販売タイムライン枠の直下に目立つアラートタグ（赤枠・ハイライトバッジ）として明記すること。
- **特典会の進行順とメニュー・テーブル順の厳格な一致**:
  - 実際の現場進行（例: フリーお手振り ➔ ①動画お手振り ➔ ②グループshot ➔ ③2shot/ソロshot）と、紙面の特典会メニュー表の行順序は必ず100%一致させること。番号順や時間順がテーブルと食い違うと、現場スタッフおよび参加者の導線混乱を招く。
- **対象商品の形態・品番（型番）の明記**:
  - 複数形態のCD/エムカード等の対象商品がある場合、商品タイトルだけでなく品番（例: `BTRC-1055`）と盤種名（例: 【集合盤】、【ユニット盤】）をタグ・バッジとして明記し、レジ前での購入間違いを防止すること。
- **会場フロアマップの縦長構成原則（ステージ上部配置と入場案内文の横幅確保）**:
  - 会場フロア図が横長の場合、横並びになる入場案内テキストの横幅を極端に圧迫し、重要な入場案内が窮屈で読みにくくなる。
  - 会場図はステージを上部（または進行方向）に配した**縦長比率（アスペクト比 3:4〜1:1.35程度）**で作成・配置すること。これにより、左側の入場案内欄（入場順ステップ、優先／女性専用エリア案内、集合ルール）に十分な横幅を確保でき、視認性と直感性が劇的に向上する。
- **長文テキストの適正改行と横幅最適化**:
  - 紙面の縦方向に余裕がある場合、長文を1行にだらだらと流さず、意味のまとまり（文脈・重要事項ごと）に適宜改行（`<br>` やブロック分離）を挟み、1行あたりの視線移動幅を最適化すること。横長すぎずゆったりとしたブロックとして見せることで、現場での速読性と理解度が格段に上がる。
- **カラー絵文字（🚨等）による Chromium / Skia PDF レンダラークラッシュ防止**:
  - Vivliostyle CLI（Puppeteer）内部の Skia / HarfBuzz グリフ解決において、特定のカラー絵文字（例: `🚨` パトランプ等）が含まれると `ProtocolError (Page.printToPDF): Printing failed` が発生し、PDFビルドがクラッシュすることがある。
  - 印刷用HTMLではクラッシュを誘発しやすいカラー絵文字の使用を避け、安定したグリフ記号（`⚠️`、`★`、`📍`、`🎁`、`🎫`、`🎤`、`📸`、`✨`、`📱` 等）やテキストラベル（`【重要】`、`【入場順】` 等）を使用すること。
- **一次情報にない事実の推測・補完の絶対禁止（事実の厳格性原則）**:
  - イベントのレギュレーション（カメラの機種制限、撮影可否、手荷物預かりの有無、飲食ルール等）やメンバー名・固有名詞について、**元情報（公式サイト・アナウンス）に明記されていない事実を「一般的だろう」と勝手に推測して明示してはならない**。
  - 例: 「撮可TIMEあり」とだけ書かれている場合に、勝手に「一眼レフ可」「カメラ機種制限なし」と断定してはならない。記載がない場合は「詳細は当日会場アナウンスをご確認ください」等に留め、公式記述の範囲を逸脱しないこと。
  - ロッカーやクロークの有無、マスク着用義務など、公式の基本注意事項に書かれている記述（「ロッカー・クロークなし」「特典会時はマスク必須」など）を読み飛ばして真逆の表記をしないよう、公式告知文の全文精査を徹底すること。
- **出演者名・メンバー名などの固有名詞の完全正確性**:
  - 出演者やメンバーの氏名は、公式プロフィール等に記載された正式表記（姓名フルネーム、正しい漢字・ひらがな表記）を厳密に用いること。略称や名字のみ、推測での表記は厳禁とする。

